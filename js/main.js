let firstNumber;
let secondNumber;

firstNumber = 10;
secondNumber = 12;

function addParams(firstNumber, secondNumber) {
  return firstNumber + secondNumber;
}

console.log(`Wynik dodawania parametrow funkcji addParams(firstNumber, secondNumber), to: ${addParams(firstNumber, secondNumber)}`);